# AI-Range-Demo 工作规范

## 项目目标

这是 AI 安全攻防演练场的前端演示原型。以可信、完整的演示链路为优先，不接入真实攻击、训练或调度系统。

## 技术与目录约定

- 应用为无构建依赖的 vanilla JavaScript hash 路由 SPA。
- `index.html` 仅承载应用外壳、导航和脚本/样式入口。
- `app.js` 负责路由与跨页面应用状态；新增页面应以 `renderXxx()` 接入。
- `data.js` 放模拟业务数据；页面运行时逻辑避免污染全局状态。
- `styles.css` 为共享样式；独立页面样式放入具名的 `*-page.css`，并使用唯一的类名前缀。
- `tokens.css` 是 DiscoveryOS 设计令牌唯一来源；不在功能样式中重复定义主题颜色或字体令牌。

## 实施与验证

- 新增页面必须接入侧栏、hash 路由与现有深浅主题。
- 路由离开时清理页面计时器，避免演示状态在后台继续执行。
- 修改后至少验证：`node --check app.js`、新增脚本的语法检查、静态资源路径与 Git diff 空白检查。
- 演示数据和界面必须明确是预置/模拟态，不表述为真实靶场、训练或调度结果。
