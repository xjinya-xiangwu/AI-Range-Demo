# 0930 三类用户产品架构与 Demo 重构方案

- **版本**：V2.0 待确认
- **日期**：2026 年 9 月 7 日
- **适用分支**：`codex/0930-ai-native-demo`
- **状态**：先冻结用户动线和信息架构，本文确认前不继续重构页面

## 1 结论

当前 Demo 的核心问题不是角色名称不够明显，而是三类用户仍然共用同一个产品骨架：同一个角色总览、同一组共享导航、同一套指标和相似的任务页面。角色切换只是隐藏或显示一个入口，没有改变用户进入平台后看到的首页、主要对象、操作顺序和成功标准。

下一版应改成**一个底座、三个独立产品空间**：

1. **研究员空间**只围绕训练任务组织。数据集、算力、评测基准和模型版本都是创建或查看训练任务时的辅助信息，不单独抢占主导航。
2. **监管空间**只保留评测任务。用户进入后直接看到评测任务列表，不需要角色首页、数据引擎、资源接入或复杂运营大屏。
3. **开发者与企业空间**围绕数据集管理和数据生产管线组织。任务、靶场、Agent 和环境都是生产数据的资源和步骤。

三类用户共享任务执行、资源目录、轨迹、证据、数据版本、权限和审计能力，但**共享能力只存在于系统底座，不意味着必须出现在每个角色的导航中**。

## 2 当前实现与目标的差距

| 维度 | 当前 Demo | 目标状态 |
|---|---|---|
| 角色入口 | 左侧下拉框切换角色 | 账号角色决定默认产品空间；Demo 身份切换放在头像菜单中 |
| 首页 | 三类用户均进入相似的角色总览 | 研究员进入训练任务；监管进入评测任务；开发者进入数据集 |
| 导航 | 三类用户共享数据引擎、场景库、资源接入 | 每类用户拥有独立且最少的导航 |
| 研究员主对象 | 数据集和消费记录 | 训练任务、训练运行、模型版本和训练效果 |
| 监管主对象 | 旧测试任务中心加结果确认 | 简单的评测任务列表、创建、运行状态和报告 |
| 开发者主对象 | 数据生产批次 | 数据集、数据版本、生产管线和管线运行 |
| 共享能力 | 作为一级菜单暴露 | 作为上下文能力嵌入角色流程 |
| 用户切换感受 | 页面配色和名称近似 | 首页、导航、对象、操作和指标均明显不同 |

### 2.1 需要停止延续的设计

- 不再提供“三类用户视角”公共首页。
- 不再把角色选择器固定放在左侧导航顶部。
- 不再要求监管用户理解数据引擎、场景库和接入网关。
- 不再把训练数据消费当成研究员的最终任务；它只是训练任务的输入步骤。
- 不再把企业用户的首页设计成运行监控页；其首要资产是数据集和数据版本。
- 不再用同一个“任务中心”页面仅修改标题来服务不同用户。

## 3 用户模型和严格边界

## 3.1 研究员

### 用户目标

创建、运行和管理训练任务，观察训练状态，比较训练前后结果，最终获得可用模型版本。

### 用户最关心的问题

1. 我的训练任务现在是什么状态？
2. 我该选择哪个数据版本、基准、模型和训练方式？
3. 训练是否正常，是否需要停止或调整？
4. 新模型相对基线是否有提升，安全指标是否恶化？
5. 本次训练使用了哪些私有数据，是否符合授权范围？

### 主对象

- TrainingTask
- TrainingRun
- ModelVersion
- TrainingResult

### 支撑对象

- DatasetVersion
- BenchmarkVersion
- ComputeResource
- TrainingConfig

### 不需要看到

- 企业数据交付
- 数据生产管线
- 监管审批和审计报告
- Agent 接入网关全量管理
- 靶场资源大厅

## 3.2 监管用户

### 用户目标

创建和执行评测任务，查看结果和证据，最终获得可信评测报告。

### 用户最关心的问题

1. 哪些评测任务正在运行、等待处理或已经完成？
2. 本次评测对象、任务集、环境和评分规则是否已经冻结？
3. 评测是否成功，是否存在异常或待人工确认项？
4. 最终结论、评分、失败项和证据是什么？
5. 报告能否复现和审计？

### 主对象

- EvaluationTask
- EvaluationRun
- EvidenceReview
- EvaluationReport

### 支撑对象

- EvaluationTarget
- TaskSetVersion
- EnvironmentVersion
- ScoringRuleVersion

### 不需要看到

- 训练任务和超参数
- 数据集管理
- 数据生产管线
- Agent 或环境接入管理
- 平台运营指标

## 3.3 开发者与企业用户

### 用户目标

管理数据资产，配置数据生产管线，通过编排好的靶场任务批量 Roll，获得经过质量和隐私门禁的数据版本。

### 用户最关心的问题

1. 当前有哪些数据集和版本，质量、来源和权限是什么？
2. 如何创建一条数据生产管线？
3. 选择哪个场景模板、Agent、环境和数据目标？
4. 管线运行了多少次，产出多少有效轨迹，失败原因是什么？
5. 新数据版本是否通过质量与隐私门禁，能否交付或导出？

### 主对象

- Dataset
- DatasetVersion
- ProductionPipeline
- PipelineRun

### 支撑对象

- ScenarioTemplate
- AgentConnection
- EnvironmentConnection
- QualityGate
- DeliveryRecord

### 不需要看到

- 训练超参数和 GPU 监控
- 监管报告签发流程
- 研究员模型版本排行
- 综合态势大屏

## 4 三类用户的端到端动线

## 4.1 研究员训练动线

```text
登录
  → 训练任务列表
  → 新建训练任务
  → 选择基础模型和训练目标
  → 选择已授权数据版本和固定基准
  → 配置训练方法、资源和关键参数
  → 确认隐私范围和执行计划
  → 启动训练
  → 查看任务详情和实时指标
  → 查看训练结果和基准对比
  → 发布或保留模型版本
```

### 核心页面

1. **训练任务**：默认首页，显示运行中、排队、评估中、已完成任务。
2. **新建训练任务**：六步以内的向导，数据选择只是其中一步。
3. **训练任务详情**：任务信息、训练阶段、核心指标、GPU、日志、异常和停止操作。
4. **训练结果**：固定基准对比、安全指标、数据版本、模型版本和发布状态。

### 首页指标

- 运行中训练任务
- 排队任务
- 待评估任务
- 本周完成任务
- 最近训练模型版本

### 研究员的 930 最小闭环

选择数据版本并创建训练任务 → 查看运行状态 → 查看训练结果 → 生成模型版本。

## 4.2 监管评测动线

```text
登录
  → 评测任务列表
  → 新建评测任务
  → 选择待测模型或 Agent
  → 选择冻结的任务集和环境
  → 确认评分规则
  → 启动评测
  → 查看运行状态
  → 处理待确认结果
  → 查看或导出评测报告
```

### 核心页面

1. **评测任务**：唯一一级页面，包含状态筛选和新建按钮。
2. **新建评测任务**：最简单的三步或四步表单。
3. **评测任务详情**：冻结信息、进度、异常、待确认项和结果摘要。
4. **评测报告**：评分、能力维度、失败项、证据和版本快照。

### 首页指标

监管用户不需要独立指标首页。任务列表顶部只显示：

- 运行中
- 待确认
- 已完成
- 失败

### 监管的 930 最小闭环

创建一个冻结评测任务 → 运行 → 确认争议结果 → 生成报告。

## 4.3 开发者与企业数据动线

### 数据集管理动线

```text
登录
  → 数据集列表
  → 查看数据集
  → 查看数据卡和版本
  → 检查来源、质量、隐私和允许用途
  → 创建新版本或交付版本
```

### 数据生产动线

```text
数据集列表
  → 创建生产管线
  → 定义目标数据集或新版本
  → 选择场景任务模板
  → 选择或接入 Agent 和环境
  → 配置 Roll、并发、预算和停止条件
  → 配置质量与隐私门禁
  → 启动管线
  → 查看 Pipeline Run
  → 处理失败与隔离轨迹
  → 生成 DatasetVersion
  → 交付或受控导出
```

### 核心页面

1. **数据集**：默认首页，展示数据集、版本、规模、质量、隐私和最近更新。
2. **数据集详情**：数据卡、版本记录、来源管线、质量报告、授权和消费记录。
3. **生产管线**：管线列表、模板、运行状态和产出。
4. **创建管线**：数据目标、任务模板、Agent、环境、Roll、门禁和预算。
5. **管线详情**：步骤状态、运行批次、有效轨迹、失败轨迹、成本和输出版本。
6. **资源接入**：作为次级页面，管理 Agent 和环境连接。

### 首页指标

- 数据集数量
- 正式数据版本
- 今日有效轨迹
- 运行中管线
- 质量通过率
- 待交付版本

### 开发者与企业的 930 最小闭环

选择或创建目标数据集 → 创建生产管线 → 批量 Roll → 通过门禁 → 生成新数据版本。

## 5 新的信息架构

## 5.1 登录和身份

真实产品中，角色由账号权限决定。Demo 为方便演示，可以保留身份切换，但应放在右上角头像菜单中，命名为“切换演示身份”。切换后重新加载对应产品空间，不能只隐藏一个菜单项。

```text
SSO 登录
├─ 研究员身份 → /research/training
├─ 监管身份   → /regulatory/evaluations
└─ 企业身份   → /developer/datasets
```

## 5.2 研究员空间导航

```text
训练任务
└─ 个人与权限
```

为了突出训练，0930 不增加独立的“模型中心”“数据中心”和“资源中心”。模型版本、数据选择、训练结果都放在训练任务上下文中。

## 5.3 监管空间导航

```text
评测任务
└─ 个人与权限
```

监管是最简单的产品空间。评测报告、证据确认、任务运行均从评测任务进入，不设其他业务一级菜单。

## 5.4 开发者与企业空间导航

```text
数据集
生产管线
资源接入
个人与权限
```

场景模板不需要独立一级“靶场大厅”。创建管线时直接选择任务模板；需要浏览时使用管线页面内的模板库抽屉或二级页。

## 5.5 不再提供的公共导航

- 角色总览
- 态势感知
- 数据引擎
- 场景与任务库

这些能力改为角色空间内的页面、步骤或底层服务，不再作为所有用户都能看到的公共模块。

## 6 页面架构和对象关系

```text
研究员空间
TrainingTask
├─ BaseModelVersion
├─ DatasetVersion
├─ BenchmarkVersion
├─ TrainingConfig
├─ TrainingRun
└─ TrainingResult → ModelVersion

监管空间
EvaluationTask
├─ EvaluationTarget
├─ TaskSetVersion
├─ EnvironmentVersion
├─ ScoringRuleVersion
├─ EvaluationRun
├─ EvidenceReview
└─ EvaluationReport

开发者与企业空间
Dataset
├─ DatasetVersion
├─ DataCard
├─ QualityReport
├─ PermissionPolicy
└─ ProductionPipeline
   ├─ ScenarioTemplate
   ├─ AgentConnection
   ├─ EnvironmentConnection
   ├─ PipelineRun
   ├─ StepTrace / Evidence
   └─ Output DatasetVersion
```

## 7 共享系统底座

共享底座应在代码和数据模型中复用，但不形成统一用户首页。

| 底座能力 | 研究员使用方式 | 监管使用方式 | 开发者与企业使用方式 |
|---|---|---|---|
| 身份与项目隔离 | 私有训练项目 | 监管评测项目 | 企业数据项目 |
| 资源目录 | 创建训练时选择模型、数据和算力 | 创建评测时选择冻结对象和环境 | 创建管线时选择 Agent、环境和模板 |
| 任务编排 | TrainingRun | EvaluationRun | PipelineRun |
| 轨迹与证据 | 训练样本来源和异常诊断 | 评测结论证据 | 数据质量与复现 |
| 数据版本 | 训练输入 | 仅在报告中引用 | 主要交付对象 |
| 权限与审计 | 私有数据用途 | 版本冻结与报告审计 | 数据权属、交付与导出 |

## 8 现有 Demo 资产重组

## 8.1 研究员空间

| 现有能力 | 处理方式 |
|---|---|
| 训练任务列表 | 作为研究员默认首页保留并强化 |
| 新建训练向导 | 保留；把数据授权、固定基准和隐私确认补入流程 |
| 训练实时监控弹窗 | 保留为任务详情的运行中状态 |
| 已完成训练任务 | 改造成训练结果和模型版本入口 |
| 训练数据消费页面 | 删除独立一级入口；能力并入新建任务和任务详情 |
| 训练大屏排行 | 删除或降级，不作为研究员核心任务 |

## 8.2 监管空间

| 现有能力 | 处理方式 |
|---|---|
| 测试任务列表 | 直接改造成评测任务默认首页 |
| 新建测试任务 | 缩减成监管评测表单，只保留待测对象、任务集、环境和评分规则 |
| 运行中任务 | 保留进度和异常，不展示企业数据生产参数 |
| 结果确认 | 放入评测任务详情，不在列表页堆叠全部工单 |
| 报告闸门 | 保留 |
| 靶场大厅、接入网关、数据中心 | 对监管用户完全隐藏 |

## 8.3 开发者与企业空间

| 现有能力 | 处理方式 |
|---|---|
| 旧数据中心 | 重构为数据集列表和数据集详情 |
| 当前数据生产页面 | 重构为生产管线列表和 Pipeline Run |
| 靶场大厅 | 改为创建管线中的任务模板选择器 |
| 接入网关 | 改为资源接入，保留 Agent 和环境连接 |
| 轨迹数据、风险数据 | 变成 Dataset 和 DatasetVersion 示例 |
| 质量与隐私门禁 | 放入管线配置和数据版本详情 |

## 9 Demo 路由规划

```text
/login

/research/training
/research/training/new
/research/training/:id
/research/training/:id/result

/regulatory/evaluations
/regulatory/evaluations/new
/regulatory/evaluations/:id
/regulatory/evaluations/:id/report

/developer/datasets
/developer/datasets/:id
/developer/pipelines
/developer/pipelines/new
/developer/pipelines/:id
/developer/connections

/account
```

旧路由在 Demo 迭代期可以暂时重定向：

| 旧路由 | 新路由 |
|---|---|
| `#/training` | `#/research/training` |
| `#/training-live` | 当前训练任务详情弹窗或 `#/research/training/:id` |
| `#/tasks` | `#/regulatory/evaluations` |
| `#/data` | `#/developer/datasets` |
| `#/production` | `#/developer/pipelines` |
| `#/gateway` | `#/developer/connections` |
| `#/range-hall` | 不再作为一级路由；过渡期跳到新建管线 |
| `#/workspace` | 根据当前演示身份跳转到对应默认首页 |

## 10 Demo 视觉和交互差异

角色差异首先由内容和动线表达，不依靠更换颜色制造差异。

### 研究员

- 高密度任务列表和训练状态。
- 主按钮始终是“新建训练任务”。
- 任务详情突出训练阶段、核心指标、日志和模型结果。
- 数据集以选择器、数据卡摘要或任务详情关联项出现。

### 监管

- 最简洁的任务列表。
- 主按钮是“新建评测任务”。
- 页面不展示成本、Token、数据产量、GPU 和资源管理。
- 报告和证据优先，运行监控只显示必要状态。

### 开发者与企业

- 数据资产管理界面，默认显示数据集和版本。
- 主按钮是“创建数据集”或“创建生产管线”。
- 管线使用步骤式或节点式状态表达。
- 突出数据质量、有效轨迹、成本、隐私和交付状态。

## 11 0930 Demo 范围

## 11.1 必须完成

### 研究员

- 训练任务默认首页。
- 新建训练任务完整向导。
- 数据版本与隐私作为训练配置步骤。
- 训练任务详情和运行监控。
- 已完成任务结果和模型版本摘要。

### 监管

- 单一评测任务首页。
- 简化的新建评测任务。
- 任务详情、待确认项和报告。
- 冻结对象、任务集、环境和评分规则的展示。

### 开发者与企业

- 数据集默认首页。
- 数据集详情和版本列表。
- 生产管线列表。
- 创建管线向导。
- Pipeline Run 详情和输出数据版本。
- Agent 与环境连接列表。

### 系统

- 三套独立导航。
- 演示身份切换。
- 旧路由兼容重定向。
- 共享样例数据和统一状态保存。
- 三条端到端验收脚本。

## 11.2 不做

- 不建设新的角色总览页。
- 不建设监管综合大屏。
- 不建设研究员独立数据集管理。
- 不建设完整训练后端、真实 GPU 调度和 MLOps。
- 不建设可自由拖拽的通用管线编辑器。
- 不从自然语言自动生成未知攻击代码或真实外部网络。
- 不为每个角色复制一套相同组件和数据。

## 12 工程重构方案

0930 继续使用现有原生 HTML、CSS、JavaScript，不切换 React，不引入新依赖。先解决产品架构，再解决技术栈。

### 12.1 建议文件结构

```text
index.html
app.js                   # 启动、登录、路由和兼容跳转
role-config.js           # 三类身份、默认路由和导航
research.js              # 训练任务页面和交互
regulatory.js            # 评测任务页面和交互
developer.js             # 数据集、管线和连接页面
shared.js                # 弹窗、表格、格式化、状态存取
 data.js                  # 共用演示数据，逐步按领域整理
styles.css
role-spaces.css          # 三套空间的布局差异
```

### 12.2 重构原则

1. 不一次性重写 6,000 多行 `app.js`。
2. 先建立角色路由和独立导航，再逐页迁移现有函数。
3. 每迁移一个页面，旧函数停止被路由调用；完成全部迁移后再删除死代码。
4. 共用任务状态、弹窗和表格方法，不复制实现。
5. 三类角色的数据访问必须通过角色配置判断，不能只依赖 CSS 隐藏。
6. Demo 的身份切换保存在浏览器本地；真实权限边界在文档中明确为后端职责。

## 13 实施顺序

### 第一步 角色外壳

- 删除公共角色总览。
- 将身份切换移入头像菜单。
- 建立三套导航和默认路由。
- 增加旧路由重定向。

### 第二步 研究员空间

- 以现有训练任务为默认首页。
- 合并数据消费能力到训练向导。
- 调整训练任务详情和结果页。

### 第三步 监管空间

- 精简现有任务中心。
- 将结果确认移入任务详情。
- 简化创建任务流程。

### 第四步 开发者与企业空间

- 将旧数据中心改为数据集管理。
- 将数据生产批次升级为管线和 Pipeline Run。
- 将靶场与 Agent 接入放入管线创建上下文。

### 第五步 清理和验收

- 删除无入口旧页面或保留明确兼容跳转。
- 更新测试用例和 README。
- 执行三类用户端到端验收。

## 14 三条验收脚本

### 研究员

登录研究员身份 → 看到训练任务 → 新建训练 → 选择数据和基准 → 启动 → 查看运行详情 → 查看结果和模型版本。

### 监管

登录监管身份 → 看到评测任务 → 新建评测 → 冻结条件 → 启动 → 确认证据 → 生成报告。

### 开发者与企业

登录企业身份 → 看到数据集 → 进入或创建数据集 → 创建生产管线 → 选择任务模板和 Agent → Roll → 质量门禁 → 生成数据版本。

## 15 待确认决策

1. 研究员首页是否只保留“训练任务”，不设置独立模型版本导航。
2. 监管是否只保留“评测任务”和“个人与权限”两个导航项。
3. 开发者与企业的默认首页是否确定为“数据集”，而不是“生产管线”。
4. 开发者与企业导航是否使用“数据集、生产管线、资源接入”三个业务入口。
5. Demo 身份切换是否移到头像菜单，并在页面顶部显示“当前演示身份”提示。
6. 是否同意 0930 继续使用原生 JavaScript 增量重构，不切换技术栈。
